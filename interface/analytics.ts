export interface CandidateCardStat{
    total_candidates: string,
    count_applied: string,
    count_screening: string,
    count_interview: string,
    count_offer: string,
    count_hired: string,
    count_rejected: string
}