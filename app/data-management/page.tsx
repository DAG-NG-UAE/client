"use client";
import ImportTrackerDataModal from '../../components/data-management/ImportTrackerDataModal';

const ImportTrackerDataPage = () => {
  return (
    <ImportTrackerDataModal open={true} onClose={() => {}} />
  );
};

export default ImportTrackerDataPage;
